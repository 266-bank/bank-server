// need to link to database

module.exports = function(req, res) {
    app.get("/:username/balance", function(req, res) {
        // verify with what's stored in session and then get balance
    });

    app.post("/deposit", function(req, res) {
        // verify with what's stored in session and then deposit $$
    })

    app.post("/withdrawal", function(req, res) {
        // verify with what's stored in session and then withdraw $$
    })
}